# Greece Guide

Greece Guide — бесплатный справочник для жителей Греции. Он объясняет официальные греческие бюрократические процедуры простым языком и ссылается только на официальные источники.

## Установка зависимостей

```bash
npm install
```

## Локальный запуск

```bash
npm run dev
```

Откройте адрес, который покажет Vite, обычно `http://localhost:5173`.

## Сборка

```bash
npm run build
npm run preview
```

## Как добавить статью

1. Откройте `src/data/articles.js`.
2. Добавьте объект в массив `articles`.
3. Обязательные поля:
   - `id`
   - `title`
   - `category`
   - `summary`
   - `status`
   - `lastChecked`
   - `sourceName`
   - `sourceUrl`
   - `sections`
4. Используйте только категории из массива `categories`.
5. Новые статьи по умолчанию создавайте со статусом `draft`.
6. Если точной информации нет в официальном источнике, пишите: `не указано в источнике`.
7. Если требования могут меняться, пишите: `Проверьте актуальные требования на официальном сайте.`

## Официальные источники

Используйте только:

- `gov.gr` — государственные услуги
- `efka.gov.gr` — социальное страхование / e-EFKA
- `ypergasias.gov.gr` — Министерство труда и социального страхования
- `aade.gr` — налоги, AFM, TAXISnet
- `dypa.gov.gr` — работа, безработица, пособия
- `eopyy.gov.gr` — медицина, EOPYY
- `migration.gov.gr` — миграционные вопросы
- `stratologia.gr` — армия и στρατολογία
- сайты Δήμος — муниципальные услуги

Не используйте блоги, форумы, Facebook и неофициальные сайты.

## Как проверять статьи

1. Откройте официальный источник из `sourceUrl`.
2. Проверьте, что статья не добавляет юридические детали, которых нет в источнике.
3. Проверьте дату `lastChecked`.
4. Убедитесь, что спорные места отмечены как `не указано в источнике` или `Проверьте актуальные требования на официальном сайте.`
5. Только владелец проекта после ручной проверки может поменять `status` на `verified`.

## Публикация на GitHub Pages

1. Убедитесь, что репозиторий называется `Greece-guide` или измените `base` в `vite.config.js`.
2. Соберите проект для GitHub Pages:

```bash
GITHUB_PAGES=true npm run build
```

3. Опубликуйте папку `dist` через GitHub Actions или пакет `gh-pages`.
4. В настройках GitHub Pages выберите источник публикации.

## Публикация на Vercel

1. Импортируйте репозиторий в Vercel.
2. Framework Preset: `Vite`.
3. Build command: `npm run build`.
4. Output directory: `dist`.
